{
  "name": "@nichestream/worker",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nichestream/db": "workspace:*",
    "@nichestream/types": "workspace:*",
    "better-auth": "^1.0.0",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.3.0",
    "postgres": "^3.4.0",
    "stripe": "^15.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.57.0"
  }
}
